rootProject.name = 'banking-ai-mcp'

include(
    'banking-common',
    'banking-events',
    'banking-onboarding',
    'banking-account',
    'banking-payment',
    'banking-fraud',
    'banking-notification',
    'banking-ai-gateway',
    'banking-mcp-client'
)

dependencyResolutionManagement {
    repositories {
        mavenCentral()
        maven { url 'https://repo.spring.io/milestone' }
        maven { url 'https://repo.spring.io/snapshot' }
    }
}
