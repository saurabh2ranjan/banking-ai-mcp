spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        preferred_uuid_jdbc_type: varchar
  ai:
    openai:
      api-key: test-key-not-used
    mcp:
      server:
        name: banking-mcp-server
        version: 1.0.0
        type: SYNC
        capabilities:
          tool: true
  autoconfigure:
    exclude:
      # Exclude OpenAI — no real key in tests. BankingAiConfig skips its beans via
      # @ConditionalOnBean(ChatModel.class); nested AiTestConfig provides mock beans.
      - org.springframework.ai.autoconfigure.openai.OpenAiAutoConfiguration
      - org.springframework.ai.autoconfigure.chat.client.ChatClientAutoConfiguration

banking:
  security:
    api-key: test-api-key

logging:
  level:
    com.banking: INFO
    org.hibernate.SQL: WARN
